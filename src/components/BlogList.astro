---
import type { CollectionEntry } from 'astro:content';
import BlogPostMeta from '@components/BlogPostMeta.astro';
import BlogPostTitle from '@components/BlogPostTitle.astro';
import PostCover from '@components/PostCover.astro';
import Button from '@components/ui/button.astro';
import Pagination from '@components/ui/pagination.astro';
import Separator from '@components/ui/separator.astro';
import { getPostCover } from '@utils/covers';
import { getPostPreviewHtml } from '@utils/post-preview';
import { getPostUrl as getCanonicalPostUrl } from '@utils/posts';
import { proseClasses } from '@utils/prose';

interface Props {
  posts: CollectionEntry<'posts'>[];
  pageNumber: number;
  totalPages: number;
  basePath?: string;
  featuredFirst?: boolean;
}

const normalizeBasePath = (path = '') => {
  const trimmed = path.replace(/\/+$/, '');
  if (!trimmed || trimmed === '/') return '';
  return trimmed.startsWith('/') ? trimmed : `/${trimmed}`;
};

const ensureLeadingSlash = (path: string) =>
  path.startsWith('/') ? path : `/${path}`;

const basePath = normalizeBasePath((Astro.props as Props).basePath);
const {
  featuredFirst = false,
  posts,
  pageNumber,
  totalPages,
} = Astro.props as Props;

const pageHref = (page: number) =>
  page === 1 ? basePath || '/' : `${basePath || ''}/seite/${page}/`;

const previousPage =
  pageNumber > 1 ? ensureLeadingSlash(pageHref(pageNumber - 1)) : undefined;
const nextPage =
  pageNumber < totalPages
    ? ensureLeadingSlash(pageHref(pageNumber + 1))
    : undefined;

const getPostUrl = (post: CollectionEntry<'posts'>) =>
  ensureLeadingSlash(`${basePath}${getCanonicalPostUrl(post)}`);

const listedPosts = await Promise.all(
  posts.map(async (post) => {
    return {
      cover: await getPostCover(post),
      featuredPreview: getPostPreviewHtml(post, 5).join('\n'),
      post,
      summary: getPostPreviewHtml(post, 1).join('\n'),
    };
  }),
);

const shouldFeaturePost = (index: number) =>
  featuredFirst && pageNumber === 1 && index === 0;

const coverOutlineClass =
  'outline outline-1 -outline-offset-1 outline-black/10 dark:outline-white/10';
const showPagination = totalPages > 1;
const showTopPagination = showPagination && pageNumber > 1;
---

{showTopPagination && (
    <Pagination
      basePath={basePath}
      placement="top"
      previousHref={previousPage}
      nextHref={nextPage}
      pageNumber={pageNumber}
      totalPages={totalPages}
    />
  )}
<ul class="mx-auto flex max-w-4xl flex-col gap-8 px-4">
  {listedPosts.map(({ cover, featuredPreview, post, summary }, index) => (
      <li>
        {shouldFeaturePost(index) ? (
            <article class="rounded-(--radius) bg-card px-4 py-8 text-card-foreground sm:px-8">
              {cover && (
                <PostCover
                  cover={cover}
                  eager
                  figureClass="mb-6"
                  imageClass={`block h-auto w-full rounded-[calc(var(--radius)-4px)] ${coverOutlineClass}`}
                  videoClass={`block aspect-video w-full overflow-hidden rounded-[calc(var(--radius)-4px)] ${coverOutlineClass}`}
                  presentationWidth={896}
                />
              )}
              <header class="mb-8 text-center">
                <BlogPostTitle
                  title={post.data.title}
                  href={getPostUrl(post)}
                />
                <BlogPostMeta
                  date={post.data.date}
                  themen={post.data.themen}
                  class="mb-4 mt-3"
                />
                <Separator />
              </header>
              <div
                class:list={[proseClasses, 'text-pretty']}
                set:html={featuredPreview}
              >
              </div>
              <p class="m-0 mt-6 flex justify-end">
                <Button
                  href={getPostUrl(post)}
                  variant="default"
                >
                  Weiterlesen
                </Button>
              </p>
            </article>
          ) : (
            <article class="rounded-(--radius) bg-card px-4 py-6 text-card-foreground sm:px-6">
              <div
                class:list={[
                  'flex flex-col gap-4',
                  cover && (index % 2 === 0 ? 'md:flex-row-reverse' : 'md:flex-row'),
                  !cover && 'md:block',
                ]}
              >
                {cover && (
                  <PostCover
                    cover={cover}
                    figureClass="m-0 md:w-1/3 md:shrink-0"
                    imageClass={`block aspect-[4/3] h-auto w-full rounded-[calc(var(--radius)-4px)] object-cover ${coverOutlineClass}`}
                    videoClass={`block aspect-video w-full overflow-hidden rounded-[calc(var(--radius)-4px)] ${coverOutlineClass}`}
                    captionClass="sr-only"
                    sizes="(min-width: 768px) 288px, 100vw"
                  />
                )}
                <div class="min-w-0 flex-1">
                  <header class="mb-3">
                    <BlogPostTitle
                      title={post.data.title}
                      href={getPostUrl(post)}
                      size="compact"
                    />
                    <BlogPostMeta
                      date={post.data.date}
                      themen={post.data.themen}
                      class="mt-2"
                    />
                  </header>
                  <div
                    class:list={[proseClasses, 'text-pretty']}
                    set:html={summary}
                  >
                  </div>
                  <p class="m-0 flex justify-end">
                    <Button
                      href={getPostUrl(post)}
                      size="sm"
                      variant="default"
                    >
                      Weiterlesen
                    </Button>
                  </p>
                </div>
              </div>
            </article>
          )}
      </li>
    ))}
</ul>
{showPagination && (
    <Pagination
      basePath={basePath}
      placement="bottom"
      previousHref={previousPage}
      nextHref={nextPage}
      pageNumber={pageNumber}
      totalPages={totalPages}
    />
  )}
