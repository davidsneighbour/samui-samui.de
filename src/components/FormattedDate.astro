---
import { formatDate, formatPostTimestamp } from '@utils/dates';

interface Props {
  date: Date;
  extended?: boolean;
}

const { date, extended = false } = Astro.props as Props;
---

<time datetime={formatPostTimestamp(date)}
  >{formatDate(date, { extended })}</time
>
