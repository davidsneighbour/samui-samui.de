---
import { formatDate } from '@utils/dates';

interface Props {
  date: Date;
  extended?: boolean;
}

const { date, extended = false } = Astro.props as Props;
---

<time datetime={date.toISOString()}>{formatDate(date, { extended })}</time>
