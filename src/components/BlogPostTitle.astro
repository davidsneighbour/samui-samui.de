---
import { cn } from '@utils/cn';

interface Props {
  title: string;
  href?: string | undefined;
  level?: 'h1' | 'h2' | undefined;
  size?: 'default' | 'compact' | undefined;
  class?: string | undefined;
}

const {
  title,
  href,
  level = 'h2',
  size = 'default',
  class: className,
} = Astro.props as Props;
const HeadingTag = level;
const titleClass = cn(
  'm-0 text-balance font-[600] leading-tight uppercase',
  size === 'compact' ? 'text-2xl' : 'text-[1.75rem] sm:text-3xl md:text-4xl',
  className,
);
---

<HeadingTag class={titleClass}>
  {href ? (
    <a
      href={href}
      class="text-link decoration-2 underline-offset-4 transition-colors duration-150 hover:underline hover:text-primary group-hover:underline group-hover:text-primary group-focus-within:underline group-focus-within:text-primary"
    >
      {title}
    </a>
  ) : (
    title
  )}
</HeadingTag>
