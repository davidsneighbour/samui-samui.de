# ToDo's

This file is the scratchpad inbox for rough, unclear, or intentionally unprocessed
notes. GitHub Issues are the source of truth for actionable work; see `ROADMAP.md`
for the generated issue index.

Add instructions immediately. All other items add as issues. Commit new instructions with the changes to the TODO and ROADMAP files in a single commit at the end of all activity.

## Add browser starter to development server

open a browser with the dev server and the documentation server loading. via `npm run dev:open`. This should only open the browser. the dev servers must be running already.
